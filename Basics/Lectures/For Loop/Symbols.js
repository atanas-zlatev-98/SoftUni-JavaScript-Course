function symbols(input){
    let text = String(input[0]);

    for(let i = 0; i < text.length; i++){
        let letters = text[i];
        console.log(letters);
    }

}

symbols(['softuni']);
symbols(['ice cream']);